@page
@model PrivacyModel
@{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>

<p>Use this page to detail your site's privacy policy.</p>

@foreach(var item in Model.ExpenseCategoriesList)
{
    <p style="color:pink;">@Html.Raw(item.Name) -  @Html.Raw(item.Decsription)</p>
}

